import { extendTheme } from "@chakra-ui/react";
import "@fontsource/poppins";

const theme = {
  colors: {
    acsgreen: "#019378",
  },
  fonts: {
    heading: "Poppins",
    body: "Poppins",
  },
};

export default extendTheme(theme);
